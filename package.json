{
  "name": "xhs-master",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:local": "npm run env:local && next dev",
    "dev:test": "npm run env:test && next dev",
    "dev:prod": "npm run env:prod && next dev",
    "build": "next build",
    "build:local": "npm run env:local && npm run build",
    "build:test": "npm run env:test && npm run build",
    "build:prod": "npm run env:prod && npm run build",
    "build:sites": "npm run build && tsx scripts/build-sites-static.mjs",
    "build:sites:local": "npm run env:local && npm run build:sites",
    "build:sites:test": "npm run env:test && npm run build:sites",
    "build:sites:prod": "npm run env:prod && npm run build:sites",
    "start": "next start",
    "lint": "next lint",
    "env:local": "node scripts/switch-env.js local",
    "env:test": "node scripts/switch-env.js test",
    "env:prod": "node scripts/switch-env.js prod",
    "prisma:generate": "echo Prisma/SQLite runtime has been removed.",
    "prisma:migrate": "echo Prisma/SQLite runtime has been removed.",
    "prisma:seed": "echo Prisma/SQLite runtime has been removed.",
    "db:init": "echo Prisma/SQLite runtime has been removed."
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "autoprefixer": "^10.4.20",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.468.0",
    "next": "^14.2.18",
    "postcss": "^8.4.49",
    "prisma": "^5.22.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.19.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.14",
    "@types/react-dom": "^18.3.2",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.18",
    "typescript": "^5.7.2"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
